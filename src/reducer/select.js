import article from '../components/fixtures'
const initialState = {
  article,
  openItem: ''
}

export default (state = initialState, { type, payload }) => {
  switch (type) {
    default:
      return state
  }
}
